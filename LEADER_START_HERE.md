# Cofound Investment Office｜领导开始使用

这是一套可以独立放在个人电脑上运行、并由 Codex 协助使用和扩展的本地项目管理与 BP 分析系统。GitHub 保存公开程序版本；真实 BP、本地数据库、飞书定位、凭据和个人扩展不会随代码仓库同步。

## 第一次使用

1. 把整个交付文件夹解压到自己的私人工作目录，不要放入公共网盘。
2. 安装 Node.js 24 LTS；安装后重启一次电脑或终端。
3. 双击一次 `Install-Cofound-Desktop-Shortcut.cmd`。它会安装 Cofound Codex 插件、桌面入口和当前用户的登录自启动；不需要输入命令。
4. 双击桌面图标。首次启动会安装锁定版本的依赖并生成完全虚构的演示项目；以后会自动检查或恢复本地服务，然后打开看板并拉起 Codex 桌面应用。
5. 浏览器会打开 `http://127.0.0.1:4010`。真实 BP 导入后只保存在当前目录的 `data\` 中。
6. 安装完成后新开一个 Codex 对话，直接说：

   > 阅读 LEADER_START_HERE.md，并检查 plugins/cofound-bp-desk。以后协助我使用和维护这套 Cofound Investment Office；任何发布、删除、覆盖数据或修改共享范围的操作都先向我确认。

安装程序会把工程内的 `cofound-bp-desk` 注册成当前用户的本地 Codex 插件。此后新建的 Codex 对话会自动获得 Cofound 的主 Skill、辅助分析 Skill 和本地 MCP 工具；旧对话不会热加载新插件，需要新开对话。Codex 仍可以理解和修改工程源码，但分析任务会通过有边界的 Skill 与本地工具运行。

## 平时怎么用

- 双击桌面 `Co-founder Investment Office` 会检查插件版本、打开看板并拉起 Codex；开机自启只在后台启动本地服务，不会弹出浏览器。
- 如需关闭登录自启动，双击 `Disable-Cofound-Autostart.cmd`；重新运行安装脚本即可恢复。
- 浏览器看板用于导入、筛选、查看证据和管理状态；进入单个项目后点“在 Codex 中打开项目”，即可进入已经绑定该项目的空白对话，自由提问，不需要先选分析框架。
- 只有需要把一次判断固定到当前文件与事实版本并留在看板时，才展开“生成并保存结构化分析”；其中的问题框和参考视角都可以留空。
- 工作台顶部显示当前时间与最近一次数据刷新时间；这些只属于界面状态，不会写成项目字段。
- Codex 调用本地工具时会自动检查并恢复服务；首次安装失败或日志异常时，再把 `data\server.stderr.log` 交给 Codex 检查。
- 微信“文件传输助手”收件箱继续作为可选入口：发送触发词和新文件后，让 Codex 扫描并归档。
- 多项目深度判断当前先串行执行；确认同一 Codex 会话下的结果稳定后再考虑并发。分享、飞书写入、状态修改和外部发布始终单独确认。
- 需要调整字段、分析规则或页面时，在 Codex 中描述需求，让它修改后先运行 `corepack pnpm@10.34.5 release:verify`。
- 内部归档时，先让 Codex 用自然语言列出项目名称与 ID、待上传 BP/补充材料及版本、目标飞书目录，以及哪些会新增、哪些会重复跳过。看清后只需回答“确定发送”或“OK”；技术校验由系统在后台完成，同步不会覆盖或删除既有版本。
- 同事把新 BP 或更新版放入企业共享 `00_团队收件箱（待导入）` 后，在“内部资料”先点“检查新资料”，确认哪些会新增、恢复或跳过，再导入到本机。系统不会移动或删除飞书收件箱里的原件。
- 需要从日常清单移除项目时使用“移入回收站”。这只会在本机隐藏项目，原文件、分析历史、BP 版本、飞书副本和既有外部分享仍保留；可随时恢复。BP 内容更新必须作为 v2、v3 导入，不能覆盖 v1。
- 使用中遇到问题，可以打开“问题反馈”填写，也可以直接告诉自己的 Codex。Codex 会先做本机诊断，只把不含路径、日志、源码和 BP 内容的安全摘要同步给维护者；提交反馈不会自动改动正式版本。
- 只有明确选择的字段和文件版本才能发布到外部分享网站；本地完整资料库与飞书内部目录不会被整体公开。
- `data\` 与 `.env.local` 是私人数据和凭据，不发给他人、不提交到公开仓库。

## 每次维护先看什么

让 Codex 先阅读本文件、`README.md`、`docs/FEISHU_INTERNAL_STORAGE.md`、`docs/PRODUCT_FEEDBACK_AND_MAINTENANCE.md` 和 `docs/FULL_VERSION_ARCHITECTURE.md`。本地追加式 operation ledger 是统一运行记录：导入、Codex 分析、飞书同步、外部分享、产品反馈、应用更新和系统事件都按操作 ID 留痕，不允许原地修改或删除。

事实与判断必须分开。金额、轮次、日期、客户数与页码证据由程序整理；Codex 判断保存操作者、时间、模型、Skill/提示词版本、源文件 Hash 和当时的事实快照。事实或原文件变化后，旧判断保留为历史并标记过期，不得静默覆盖。

CleanTech 相关 Skill 只是按需增强：只有清洁能源、储能、光伏、电力、氢能、能效或相关基础设施项目才主动调用；非适用项目返回 `not_applicable`。飞书政策库和机会库只读查询，未通过认证或数据治理检查时应明确不可用，不能拿旧数据冒充实时结果。

## 谁来维护

GitHub `main` 和正式 Release 是公共核心版本。普通用户可以让自己的 Codex 增加本地字段、视图和用户 Skill，也可以让 Codex 诊断问题并生成候选补丁；这些操作不会自动改变官方版本。

正式维护流程是：

1. 用户或其 Codex 使用不含 BP、路径和凭据的摘要提交 Issue 或候选 PR。
2. 维护者判断是否纳入核心，并在独立分支修改。
3. 完成类型检查、测试、Skill 校验和构建。
4. 合并 `main`，发布带变更说明的新版本。
5. 使用者先备份自己的 `data\`，再升级程序；不得用空数据目录覆盖唯一备份。

领导不需要学习 GitHub 或命令行：日常仍然只使用 Codex 和看板。GitHub 解决的是核心源码版本、审核和回滚，不承接任何真实投资资料。

## Vercel 与账号的边界

交付包不会携带原开发者的 `.vercel` 绑定、`.env.local`、数据库地址、邮箱服务密钥或存储令牌。领导若只使用本地看板，不需要配置这些内容。若要管理线上协作网站，需要由公司另行给他对应的 Vercel、数据库、文件存储和邮箱验证权限，并在他的环境中单独配置。

## 交付包安全边界

自动交付包包含源码、说明、虚构样例和 Codex 插件源码；明确排除：

- `data\`、数据库、BP 原件、日志与本机备份；
- `.env.local` 与各类真实令牌；
- `.vercel` 项目绑定；
- `.git` 历史、开发缓存、构建产物和依赖目录。

收到压缩包后，可用同目录的 `.sha256.txt` 核对文件是否完整。
